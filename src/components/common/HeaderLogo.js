import React from 'react';
import { Image } from 'react-native';

export default () => (
  <Image source={require('../../assets/images/trol-logo-full.png')} style={{height: 35}} resizeMode="contain" />
);
